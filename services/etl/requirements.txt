sqlalchemy>=2.0,<2.1
greenlet>=3.5,<4.0
asyncpg>=0.31,<0.32
alembic>=1.18,<1.19
pandas>=3.0,<3.1
jinja2>=3.1,<3.2
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.2,<2.0
dbt-core==1.11.11
dbt-postgres==1.10.2
# dbt-core pins mashumaro<3.15, but that range crashes on Python 3.14
# (UnserializableField on JSONObjectSchema.schema). 3.22 fixes it and dbt
# still works fine against the small subset of mashumaro it actually uses.
mashumaro==3.22
# Pinned: newer great_expectations (>=0.18.22, and all 1.x) require numpy<2.0,
# which has no build for this Python version. 0.18.8 works with numpy 2.x.
great_expectations==0.18.8
